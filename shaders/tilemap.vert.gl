in vec4 position;
in vec2 texCoord;

out vec2 pixelCoord;

struct Viewport {
    vec2 origin;
    ivec2 size;
};

uniform Viewport vp;
uniform TextBuf buf;

void main()
{
    gl_Position = vec4(position);
    //fragColor = vec4(position.xyz * 0.5 + 0.5, 1);
    pixelCoord = texCoord * vp.size - vp.origin;
}
